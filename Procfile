# Procfile — use gunicorn and point to your project wsgi module.
# Replace `mymainserver` with your Django project package name if different.
web: gunicorn mymainserver.wsgi:application --log-file - --timeout 120
